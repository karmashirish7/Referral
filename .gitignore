# Uploaded files (user documents — not committed)
uploads/*
!uploads/.gitkeep

# Auto-generated by installer — contains DB credentials, never commit
config.php

# Local agent skills (installed per machine)
.agents/

# Environment / secrets
.env

# macOS
.DS_Store

# PHP logs
*.log
